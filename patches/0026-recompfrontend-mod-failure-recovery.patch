diff --git a/recompinput/src/input_events.cpp b/recompinput/src/input_events.cpp
index c76a1dc..e9d49e1 100644
--- a/recompinput/src/input_events.cpp
+++ b/recompinput/src/input_events.cpp
@@ -270,8 +270,9 @@ void handle_events() {
 
     binding::stop_skipping_events();
 
-    if (!started && ultramodern::is_game_started()) {
-        started = true;
+    const bool now_started = ultramodern::is_game_started();
+    if (started != now_started) {
+        started = now_started;
         recompui::process_game_started();
     }
 }
diff --git a/recompui/src/composites/ui_mod_menu.cpp b/recompui/src/composites/ui_mod_menu.cpp
index 02455fd..f94dcd0 100644
--- a/recompui/src/composites/ui_mod_menu.cpp
+++ b/recompui/src/composites/ui_mod_menu.cpp
@@ -639,17 +639,14 @@ void ModMenu::process_event(const Event &e) {
             mods_dirty = false;
             mod_scan_queued = false;
         }
-        if (ultramodern::is_game_started()) {
-            install_mods_button->set_enabled(false);
-            refresh_button->set_enabled(false);
-        }
+        // A failed mod load returns to the stopped state; restore its controls.
+        install_mods_button->set_enabled(!ultramodern::is_game_started());
+        refresh_button->set_enabled(!ultramodern::is_game_started());
         if (active_mod_index != -1) {        
             bool auto_enabled = recomp::mods::is_mod_auto_enabled(mod_details[active_mod_index].mod_id);
             bool deprecated = recomp::mods::is_mod_deprecated(mod_details[active_mod_index].mod_id, mod_details[active_mod_index].version);
             bool toggle_enabled = !auto_enabled && !deprecated && (mod_details[active_mod_index].runtime_toggleable || !ultramodern::is_game_started());
-            if (!toggle_enabled) {
-                mod_details_panel->disable_toggle();
-            }
+            mod_details_panel->get_enable_toggle()->set_enabled(toggle_enabled);
         }
     }
 }
