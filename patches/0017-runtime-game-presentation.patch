diff --git a/ultramodern/src/events.cpp b/ultramodern/src/events.cpp
index 17d7a47..82abebb 100644
--- a/ultramodern/src/events.cpp
+++ b/ultramodern/src/events.cpp
@@ -366,0 +367,6 @@ void gfx_thread_func(uint8_t* rdram, moodycamel::LightweightSemaphore* thread_re
+                // The launcher uses dummy workloads; switch only for the first game DL.
+                // Retain the port renderer's pre-HFR-launcher presentation contract.
+                if (ultramodern::is_game_started() && !enabled_instant_present) {
+                    renderer_context->enable_instant_present();
+                    enabled_instant_present = true;
+                }
