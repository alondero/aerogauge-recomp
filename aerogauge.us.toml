# AUTOGENERATED by scripts/gen_syms_toml.py -- DO NOT EDIT BY HAND.
# Whole-ROM recompile config (ROM+syms mode; see the script docstring).
# Run (see BUILDING.md step 3):
#   cmake --build build --target N64RecompCLI
#   ./build/lib/N64ModernRuntime/librecomp/N64Recomp/N64Recomp aerogauge.us.toml

[input]
entrypoint = 0x80000400
output_func_path = "RecompiledFuncs"
symbols_file_path = "aerogauge.syms.toml"
rom_file_path = "AeroGauge (USA).z64"

[patches]
stubs = [
    "func_8006AB10",
    "func_8006CCD0",
    "func_80070860",
    "func_80070880",
    "func_80070DE4",
    "func_80070ECC",
    "func_80071024",
    "func_800712F0",
    "func_80078830",
    "func_80078840",
    "func_80078900",
    "func_80078980",
    "func_8007B830",
    "func_8007BD70",
]
ignored = [
    "aeroRegisterTrackSections",
    "aeroRegisterZoneObjects",
    "guPerspectiveF",
]

# Developer warp menu (issue #3, src/aero_warp.c). Hooked at the entry of the top-level
# per-frame scene driver func_80015C8C (called every frame from the main game thread's
# loop func_800658FC): it copies the scene request 0x8013FF84 into the current scene
# 0x8013FF80 and jump-tables (0x800969C0, 10 scenes) to the active scene's runner. The
# entry hook lets a pending warp perform the menu's own race-launch stores (race-param
# block 0x8013FF90 + RACE confirm side effects) before this frame's request is consumed.
# (NOT func_80015FD0 — that is merely the RACE scene's runner, case 5 of the table, so a
# hook there goes silent in every other scene.)
[[patches.hook]]
func = "func_80015C8C"
before_vram = 0x80015C8C
text = "extern void aero_warp_tick(uint8_t*, recomp_context*); aero_warp_tick(rdram, ctx);"

# Widescreen 1P-HUD pinning (issue #1, src/aero_hud_widescreen.c). AeroGauge's HUD has no
# static per-element draw calls to bracket: the 2D dispatcher func_80022408 walks object
# lists and calls per-object handlers indirectly, and those handlers mix LEFT and RIGHT
# elements inside a single call (func_80018EA0 draws TEMP right + GLPS left), so no call
# boundary separates them. The whole dispatcher is bracketed instead: the entry hook
# latches the DL write cursor (holder 0x8016C508); the epilogue hook (before_vram
# 0x80022680, after every child handler has appended, before the register restores)
# post-processes the emitted range --
#   1. re-emits it with RT64 rect-align + wide-scissor brackets around runs of texrects,
#      each rect classified LEFT/RIGHT/centred by its own coordinates (race scene only);
#   2. shifts the speedo needle's G_MTX translate.x (matrix-rotated geometry that a
#      rect-align cannot carry), pinpointed by its G_DL -> static mesh 0x800995C0.
# Scaled/tagged so the whole pass is a no-op at 4:3 / hr_option Original.
[[patches.hook]]
func = "func_80022408"
before_vram = 0x80022408
text = "extern void aero_ws_hud_scan_begin(uint8_t*, recomp_context*); aero_ws_hud_scan_begin(rdram, ctx);"

[[patches.hook]]
func = "func_80022408"
before_vram = 0x80022680
text = "extern void aero_ws_hud_frame_end(uint8_t*, recomp_context*); aero_ws_hud_frame_end(rdram, ctx);"
